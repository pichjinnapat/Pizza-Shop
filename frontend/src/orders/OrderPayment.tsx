import React, { FunctionComponent } from 'react'
import Layout from '../components/Layout'

const OrderPayment: FunctionComponent = () => {
  return <Layout>OrderPayment</Layout>
}

export default OrderPayment
